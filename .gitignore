# === Geheimen & persoonsgegevens (NOOIT naar GitHub) ===
.env
.env.*
# config.json bevat dashboard-wachtwoord/secret + adviseur-persoonsgegevens.
# Ook ALLE varianten (config.json.bak/.backup/.oud): een handmatige kopie voor een edit stond
# anders untracked klaar om per ongeluk mee te committen (15-7: dat gebeurde bijna).
config.json
config.json.*
config.*.json
*secret*
*credential*
magicplan_forms_backup.json
magicplan_fields_backup.json

# === Projectdata / opnames (AVG: adressen, BAG, foto's, dossiers) ===
out/

# === Gelicentieerde kennisbankbronnen (NTA/ISSO/VABI e.d.; niet publiceren) ===
knowledge/private/
knowledge/**/*.pdf
knowledge/**/*.xlsx
knowledge/**/*.xls
knowledge/**/*.docx

# === Vabi/EPA-bestanden (kunnen versleuteld/persoonsgebonden zijn) ===
*.epa
*.epa.bak

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/

# === OS / editor (incl. Word/Excel tijdelijke ~$-bestanden) ===
.DS_Store
Thumbs.db
desktop.ini
*.tmp
~$*

# === Claude/agent lokale state ===
.claude/
.verify-report.json
review.md
